''' intializing and storing values in list
    range() of numbers
    taking input from user
    stored the even
    multiplication table
    ch:ord(ch)=even store the characters
    if numbers are even store their characters from the user
